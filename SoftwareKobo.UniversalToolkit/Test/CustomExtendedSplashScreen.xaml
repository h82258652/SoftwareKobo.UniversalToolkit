<controls:ExtendedSplashScreenContent x:Class="Test.CustomExtendedSplashScreen"
                                      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                                      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                                      xmlns:local="using:Test"
                                      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                                      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                                      xmlns:controls="using:SoftwareKobo.UniversalToolkit.Controls"
                                      mc:Ignorable="d"
                                      d:DesignHeight="300"
                                      d:DesignWidth="400">
    <Grid Background="SkyBlue">
        <StackPanel HorizontalAlignment="Center"
                    VerticalAlignment="Center">
            <TextBlock Text="这里是扩展启动屏幕的说"
                       FontSize="{ThemeResource TextStyleExtraLargeFontSize}" />
            <ProgressRing IsActive="True" />
            <TextBlock Text="正在启动..."
                       TextAlignment="Center" />
        </StackPanel>
    </Grid>
</controls:ExtendedSplashScreenContent>