<controls:ExtendedSplashScreenContent x:Class="Test.Controls.CustomExtendedSplashScreen"
                                      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                                      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                                      xmlns:local="using:Test.Controls"
                                      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                                      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                                      xmlns:controls="using:SoftwareKobo.UniversalToolkit.Controls"
                                      mc:Ignorable="d"
                                      d:DesignHeight="300"
                                      d:DesignWidth="400">
    <Grid>
        <StackPanel HorizontalAlignment="Center"
                    VerticalAlignment="Bottom"
                    Margin="0,0,0,150">
            <Viewbox Width="75"
                     Height="75">
                <ProgressRing IsActive="True" />
            </Viewbox>
            <TextBlock Text="正在加载..."
                       TextAlignment="Center" />
        </StackPanel>
    </Grid>
</controls:ExtendedSplashScreenContent>