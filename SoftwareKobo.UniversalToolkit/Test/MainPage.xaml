<Page x:Class="Test.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:Test"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:controls="using:SoftwareKobo.UniversalToolkit.Controls"
      mc:Ignorable="d">
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <controls:ReflectionPanel x:Name="RF">
            <Image Source="http://ww2.sinaimg.cn/large/69019c4egw1euxxlyqp9zj20qo06yq4v.jpg"
                   Width="250" />
            <controls:FullWindowPopup.AttachedPopup>
                <controls:FullWindowPopup x:Name="F">
                    <Grid Width="100"
                          Height="100"
                          Background="Red">
                        <ContentControl Content="{Binding}"></ContentControl>
                    </Grid>
                </controls:FullWindowPopup>
            </controls:FullWindowPopup.AttachedPopup>
        </controls:ReflectionPanel>
        <Button Content="Test"
                Click="Button_Click"
                HorizontalAlignment="Center"
                VerticalAlignment="Bottom" />
    </Grid>
</Page>